# Trained models: ~90 MB each, well past what belongs in a git repo.
# The shipped checkpoint is published on GitHub Releases instead; see README.
checkpoints/
*.pt

# environments
.venv*/
__pycache__/
*.pyc
.pytest_cache/

# generated training data & artifacts (rebuild with comas.synthetic / comas.realpairs)
# all generated datasets, whatever they are called - these are rebuilt with
# comas.synthetic / comas.tiling and must never end up in the repo
data/
data_*/
!data_README.md
all_shots/
checkpoints/
cache/

# inference outputs
ranked*.csv
copilot_ranked.csv
holdout_*.csv
data_pos.csv
data_neg.csv

# OS / editor
.DS_Store
.vscode/
.claude/

# launchers written by the installer
Run CoMas Triage.command
Run CoMas Triage.bat
