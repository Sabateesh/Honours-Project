# Note: tkinter/Tk is not pip-installable. On macOS, Xcode's bundled Python
# ships an old Tk (8.5) with bad rendering bugs (blank/half-drawn windows).
# Use Homebrew Python (`brew install python-tk@3.11`) or the python.org
# installer, not Xcode's python3, before running `python3 -m comas.gui`.
torch>=2.0
torchvision>=0.15
scikit-learn>=1.3
pandas>=2.0
pillow>=10.0
tqdm>=4.65
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6
pyyaml>=6.0
pytesseract>=0.3.10
sentence-transformers>=2.2
opencv-python-headless>=4.8
jinja2>=3.1
pytest>=7.4
